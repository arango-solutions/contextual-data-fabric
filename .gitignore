.DS_Store
.venv/
__pycache__/
*.egg-info/
.prd-drift-queue/

# Secrets — never commit an API key / credentials
.env
.env.*

# Ontop JDBC driver (fetched at deploy time, not versioned)
deploy/ontop/jdbc/*.jar
deploy/ontop/jdbc/*.jar
nohup.out
docs/questions_answers/

# CNL exploration — local experiment doc, deliberately out of the repo until
# the spike validates it (see the escrow appendix inside for PRD re-insertion)
docs/cnl-deterministic-rail.md
docs/**/*.pdf
