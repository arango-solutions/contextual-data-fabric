# The reviewed pin: arango-solutions main as reviewed 2026-08-06, fetched via
# the PUBLIC ArthurKeen mirror because the arango-solutions repo is private —
# an unauthenticated `make install` (fresh clone, CI) must be able to resolve
# it. The 40-hex SHA is the review contract; it is identical on both remotes.
# Keep it synchronized with CI and the CC-9 dependency-pin test.
arango-sparql-py[nl,analyzer] @ git+https://github.com/ArthurKeen/arango-sparql-py.git@603db8efeb5cd40e570d53964247ca65a80e739b
