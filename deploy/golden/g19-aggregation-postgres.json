{
  "name": "g19 aggregation — single-leg GROUP BY ships verbatim to Ontop (accounts per tier)",
  "sparql": "PREFIX c: <urn:arango-sparql:concept#> SELECT ?tier (COUNT(?a) AS ?n) WHERE { ?a a c:Account ; c:currentProductTier ?tier } GROUP BY ?tier",
  "expect": {
    "status": "grounded",
    "anchor_kind": "postgresql",
    "sources_touched": ["postgresql:crm"],
    "bindings": [
      {"tier": "Enterprise", "n": 2},
      {"tier": "ArangoGraph", "n": 1}
    ]
  }
}
